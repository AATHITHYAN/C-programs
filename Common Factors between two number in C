//Print the common factors between the numbers:

#include <stdio.h>

void comfac(int a,int b)
{
    int i,s;
    
   /* if(a>b)
        s=a;
    else
        s=b;*/
    
    //printf("%d",s);
    
    for(i=1;i<=a&&i<=b;i++)
    {
        if(a%i==0 && b%i==0)
        {
            printf("%d ",i);
        }
    }
}

int main()
{
    int a,b;
    
    scanf("%d %d",&a,&b);
    
    comfac(a,b);
}
