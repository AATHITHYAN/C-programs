#include <stdio.h>
#include <stdlib.h>

void calculate(int n)
{
    int count = 0,i;

    for(i=5; n/i >= 1;i*=5)
        count += n/i;

    printf("%d",count);
}

int main()
{
    int num;
    scanf("%d",&num);

    calculate(num);
    return 0;
}
