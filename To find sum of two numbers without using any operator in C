#include <stdio.h> 
  
int add(int number1, int number2) 
{ 
    return printf("%*c%*c", number1, ' ', number2, ' '); 
} 
  
int main() 
{ 
    printf("Sum of the two number is = %d", add(3, 4)); 
    return 0; 
} 
